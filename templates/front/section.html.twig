{% extends 'base.html.twig' %}

{% block title %}{{ section.title }} — Centre d'aide StudioAstra{% endblock %}
{% block meta_description %}{{ section.description ?? 'Consultez les articles de la section ' ~ section.title ~ ' dans le centre d\'aide StudioAstra.' }}{% endblock %}

{% block body %}
{# ── Breadcrumbs ── #}
{% include 'components/breadcrumbs.html.twig' with {breadcrumbs: [
    {label: 'Accueil', url: path('front_home')},
    {label: section.title, url: null}
]} %}

{# ── Section Header ── #}
<section class="mx-auto max-w-7xl px-4 pt-8 pb-4">
    <div class="flex items-start gap-4">
        <div class="flex h-14 w-14 shrink-0 items-center justify-center rounded-2xl bg-[#635bff]/10 text-[#635bff]">
            {% if section.icon is defined and section.icon %}
                <span class="text-2xl">{{ section.icon }}</span>
            {% else %}
                <svg xmlns="http://www.w3.org/2000/svg" class="h-7 w-7" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M3 7v10a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2h-6l-2-2H5a2 2 0 0 0-2 2z"/>
                </svg>
            {% endif %}
        </div>
        <div>
            <h1 class="font-satoshi text-3xl font-extrabold text-[#2d3748]">{{ section.title }}</h1>
            {% if section.description is defined and section.description %}
                <p class="mt-2 text-base text-[#2d3748]/60">{{ section.description }}</p>
            {% endif %}
        </div>
    </div>
</section>

{# ── Subsection Cards Grid ── #}
<section class="mx-auto max-w-7xl px-4 py-8">
    {% if section.subsections|length > 0 %}
        <div class="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
            {% for sub in section.subsections %}
                <div class="rounded-2xl border border-[rgba(0,0,0,0.06)] bg-white p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-md">
                    <a href="{{ path('front_subsection', {sectionSlug: section.slug, subsectionSlug: sub.slug}) }}"
                       class="block">
                        <h2 class="font-satoshi text-lg font-bold text-[#2d3748] hover:text-[#635bff]">
                            {{ sub.title }}
                        </h2>
                    </a>
                    {% if sub.description is defined and sub.description %}
                        <p class="mt-1 text-sm text-[#2d3748]/60">{{ sub.description }}</p>
                    {% endif %}
                    <p class="mt-3 text-xs font-medium text-[#2d3748]/40">
                        {{ sub.articles|length }} article{{ sub.articles|length > 1 ? 's' : '' }}
                    </p>

                    {# Article Links #}
                    {% if sub.articles|length > 0 %}
                        <ul class="mt-4 space-y-2 border-t border-[rgba(0,0,0,0.06)] pt-3">
                            {% for article in sub.articles %}
                                <li>
                                    <a href="{{ path('front_article', {sectionSlug: section.slug, subsectionSlug: sub.slug, articleSlug: article.slug}) }}"
                                       class="flex items-center gap-2 text-sm text-[#2d3748]/70 hover:text-[#635bff]">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 shrink-0 text-[#2d3748]/30" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                            <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h6m-6 4h6m2 5H7a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5.586a1 1 0 0 1 .707.293l5.414 5.414a1 1 0 0 1 .293.707V19a2 2 0 0 1-2 2z"/>
                                        </svg>
                                        {{ article.title }}
                                    </a>
                                </li>
                            {% endfor %}
                        </ul>
                    {% endif %}
                </div>
            {% endfor %}
        </div>
    {% else %}
        <div class="py-16 text-center">
            <p class="text-lg text-[#2d3748]/50">Aucune sous-section pour le moment.</p>
        </div>
    {% endif %}
</section>
{% endblock %}
